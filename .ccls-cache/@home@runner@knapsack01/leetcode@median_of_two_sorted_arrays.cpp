#include "bits/stdc++.h"
using namespace std;
/*
------------|-------------
------------------|-----------------
*/
class Solution {
public:
  double findMedianSortedArrays(vector<int> &nums1, vector<int> &nums2) {
    // create partition 
    int partition = (nums1.size() + nums2.size() + 1)/2;
    
    int maxRight

    // If found

    // maxLeftX > minRightY

    // else
  }
};

int main(){
  
  return 0;
}